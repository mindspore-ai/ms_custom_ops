#operator reduce_sum_batch_invariant
reduce_sum_batch_invariant:
    args:
        input:
            dtype: tensor
        dims:
            dtype: tuple[int]
        keep_dims:
            dtype: bool
        output_dtype:
            dtype: TypeId
            arg_handler: dtype_to_type_id
    returns:
        out:
            dtype: tensor
